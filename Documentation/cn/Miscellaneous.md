# MISCELLANEOUS
## Custom Greeting Text

覆盖你自己的玩家卡片上显示的问候语。

**注意: 仅在客户端生效。**

## Hide user ID

在视觉上隐藏你的 UID，使其不会出现在视频或截图中。

**注意: 仅在客户端生效，不会从 ProjectMoon 的服务器中隐藏你的 UID。**

## Disable Liquid Glass

禁用 tweak 的 liquid glass，并将其替换为基于 blur 的 UI 设计。

需要重启应用后才会生效。
